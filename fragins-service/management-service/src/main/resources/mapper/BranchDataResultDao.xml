<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sinosoft.fragins.management.dao.BranchDataResultDao">
    <select id="selectByBranchNameAnddataWeidu" resultType="com.sinosoft.fragins.management.po.BranchDataResult">
        select
        *
        from branch_data_result
        where branch_name = #{branchName} and data_weidu = #{dataWeidu}
    </select>
    <select id="selectGroupByBranchNameAnddataWeidu" resultType="com.sinosoft.fragins.management.po.BranchDataResult">
        select
        *
        from branch_data_result
        group by branch_name,data_weidu
    </select>

    <delete id="deleteAll">
        delete from branch_data_result
    </delete>

    <insert id="insertBatch" >
        insert into
        branch_data_result
        (
        id,
        branch_name,
        data_weidu,
        data_type,
        deal_pin,
        deal_amt,
        deal_parent_id,
        arpu,
        pin_count,
        parent_id_amt
        )
        values
        <foreach collection="list" item="item" index="index" separator=",">
            (
            #{item.id},
            #{item.branchName},
            #{item.dataWeidu},
            #{item.dataType},
            #{item.dealPin},
            #{item.dealAmt},
            #{item.dealParentId},
            #{item.arpu},
            #{item.pinCount},
            #{item.parentIdAmt}
            )
        </foreach>
    </insert>


    <update id="updateBatch" parameterType="java.util.List">
        <foreach collection="list" item="item" index="index" open="" close="" separator=";">
            update branch_data_result
            <set>
                branch_name=#{item.branchName},
                data_weidu=#{item.dataWeidu},
                data_type=#{item.dataType},
                deal_pin=#{item.dealPin},
                deal_amt=#{item.dealAmt},
                deal_parent_id=#{item.dealParentId},
                arpu=#{item.arpu},
                pin_count=#{item.pinCount},
                parent_id_amt=#{item.parentIdAmt}
            </set>
            where
            id = #{item.id}
        </foreach>
    </update>

</mapper>